# Checkout Web (Legacy)

Next.js + Paddle.js checkout prototipi. Üretim checkout akışı `extension/checkout.html` ve `../../backend/` üzerinden çalışır.

```bash
cp .env.example .env.local
npm install
npm run dev -- -p 3001
```

Port `3001` kullanılır (`web/auth` ile çakışmayı önlemek için).
