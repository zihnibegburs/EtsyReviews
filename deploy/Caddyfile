# Domain — .env içindeki API_DOMAIN ile aynı olmalı
# Statik sayfalar da bu domain üzerinden servis edilir (örn. /pricing, /terms)
# Örnek: api.etsyfetcher.shop
{$API_DOMAIN} {
    encode gzip
    reverse_proxy backend:8081
}
